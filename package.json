{
  "name": "entity-accum-poc",
  "private": true,
  "scripts": {
    "localnet": "sui start",
    "test:move": "cd packages/world && sui move test -e testnet",
    "publish:local": "cd packages/world && sui client test-publish --gas-budget 200000000 --build-env testnet",
    "dev": "pnpm --filter entity-accum-app dev"
  }
}
